<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= form.label :user_id %>
    <%#= form.text_field :user_id, id: :order_user_id %>
    <%#= current_user  %>
  </div>
  <%= form.hidden_field :user_id %>
<%= form.hidden_field :product_id %>



  <div class="field">
    <%= form.label :shipping_postcode %>
    <%= form.number_field :shipping_postcode, id: :order_shipping_postcode %>
  </div>

  <div class="field">
    <%= form.label :shipping_address %>
    <%= form.text_area :shipping_address, id: :order_shipping_address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.number_field :phone, id: :order_phone %>
  </div>

  <div class="field">
    <%= form.label :stripe_confirmation_id %>
    <%= form.text_field :stripe_confirmation_id, id: :order_stripe_confirmation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
